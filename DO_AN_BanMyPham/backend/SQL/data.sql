CREATE TABLE `PROVIDERS` (
  `ID` VARCHAR(50) PRIMARY KEY,
  `NAME` NVARCHAR(50),
  `PHONE` VARCHAR(15),
  `ADDRESS` NVARCHAR(255),
  `EMAIL` VARCHAR(100),
  `STATUS` NVARCHAR(30)
);

CREATE TABLE `ROLES` (
  `ID` VARCHAR(10) PRIMARY KEY,
  `NAME` NVARCHAR(50),
  `DESCRIPTION` NVARCHAR(255)
);

CREATE TABLE `PERMISSION` (
  `ID` VARCHAR(10) PRIMARY KEY,
  `NAME` NVARCHAR(30),
  `DESCRIPTION` NVARCHAR(255)
);

CREATE TABLE `ROLE_PERMISSIONS` (
  `ROLE_ID` VARCHAR(10),
  `PERMISSION_ID` VARCHAR(30),
  PRIMARY KEY (`ROLE_ID`, `PERMISSION_ID`)
);

CREATE TABLE `EMPLOYEES` (
  `ID` VARCHAR(10) PRIMARY KEY,
  `NAME` NVARCHAR(50),
  `PHONE` VARCHAR(10),
  `EMAIL` VARCHAR(100),
  `ADDRESS` NVARCHAR(255)
);

CREATE TABLE `IMPORT` (
  `ID` VARCHAR(10) PRIMARY KEY,
  `PROVIDER_ID` VARCHAR(10),
  `EMPLOYEE_ID` VARCHAR(10),
  `DATE_CREATE` DATE,
  `TOTAL` FLOAT
);

CREATE TABLE `BRANDS` (
  `ID` VARCHAR(10) PRIMARY KEY,
  `NAME` NVARCHAR(50),
  `STATUS` VARCHAR(50),
  `DESCRIPTION` NVARCHAR(255)
);

CREATE TABLE `PRODUCTS` (
  `ID` VARCHAR(10) PRIMARY KEY,
  `BRAND_ID` VARCHAR(10),
  `CATEGORY_ID` VARCHAR(10),
  `NAME` NVARCHAR(50),
  `PRICE` FLOAT,
  `QUANTITY` INT,
  `STATUS` NVARCHAR(30)
);

CREATE TABLE `DISCOUNTS` (
  `ID` VARCHAR(10) PRIMARY KEY,
  `NAME` NVARCHAR(50),
  `CONDITION` VARCHAR(50),
  `START_DATE` DATE,
  `END_DATE` DATE,
  `PERCENT` FLOAT
);

CREATE TABLE `IMPORT_DEATAIL` (
  `PRODUCT_ID` VARCHAR(10),
  `IMPORT_ID` VARCHAR(10),
  `UNIT_PRICE` FLOAT,
  `QUANTITY` INT,
  PRIMARY KEY (`IMPORT_ID`, `PRODUCT_ID`)
);

CREATE TABLE `CUSTOMERS` (
  `ID` VARCHAR(10) PRIMARY KEY,
  `NAME` NVARCHAR(50),
  `PHONE` VARCHAR(10),
  `ADDRESS` NVARCHAR(255),
  `EMAIL` VARCHAR(100),
  `DATE_CREATE` DATE,
  `PASSWORD` VARCHAR(30),
  `STATUS` NVARCHAR(30)
);

CREATE TABLE `EXPORT` (
  `ID` VARCHAR(10) PRIMARY KEY,
  `DISCOUNT_ID` VARCHAR(10),
  `CUSTOMER_ID` VARCHAR(10),
  `EMPLOYEE_ID` VARCHAR(10),
  `DATE_CREATE` DATE,
  `TOTAL` FLOAT,
  `STATUS` VARCHAR(50)
);

CREATE TABLE `EXPORT_DEATAIL` (
  `EXPORT_ID` VARCHAR(10),
  `PRODUCT_ID` VARCHAR(10),
  `UNIT_PRICE` FLOAT,
  `QUANTITY` INT,
  PRIMARY KEY (`EXPORT_ID`, `PRODUCT_ID`)
);

CREATE TABLE `ACCOUNTS` (
  `USERNAME` varchar(10) PRIMARY KEY,
  `PASSWORD` varchar(10),
  `ROLE_ID` varchar(10),
  `DATE_CREATE` DATE
);

CREATE TABLE `CATEGORY` (
  `ID` varchar(10) PRIMARY KEY,
  `NAME` varchar(100),
  `DESCRIPTION` varchar(255),
  `STATUS` VARCHAR(50)
);

ALTER TABLE `PRODUCTS` ADD CONSTRAINT `CATEGORY_ID_FK` FOREIGN KEY (`CATEGORY_ID`) REFERENCES `CATEGORY` (`ID`);

ALTER TABLE `ROLE_PERMISSIONS` ADD CONSTRAINT `ROLE_FK` FOREIGN KEY (`ROLE_ID`) REFERENCES `ROLES` (`ID`);

ALTER TABLE `ROLE_PERMISSIONS` ADD CONSTRAINT `PERMISSION_ID_FK` FOREIGN KEY (`PERMISSION_ID`) REFERENCES `PERMISSION` (`ID`);

ALTER TABLE `IMPORT` ADD CONSTRAINT `PROVIDER_ID_FK` FOREIGN KEY (`PROVIDER_ID`) REFERENCES `PROVIDERS` (`ID`);

ALTER TABLE `IMPORT` ADD CONSTRAINT `EMPLOYEE_ID_FK` FOREIGN KEY (`EMPLOYEE_ID`) REFERENCES `EMPLOYEES` (`ID`);

ALTER TABLE `PRODUCTS` ADD CONSTRAINT `BRAND_ID_FK` FOREIGN KEY (`BRAND_ID`) REFERENCES `BRANDS` (`ID`);

ALTER TABLE `IMPORT_DEATAIL` ADD CONSTRAINT `PRODUCT_ID_FK` FOREIGN KEY (`PRODUCT_ID`) REFERENCES `PRODUCTS` (`ID`);

ALTER TABLE `IMPORT_DEATAIL` ADD CONSTRAINT `IMPORT_ID_FK` FOREIGN KEY (`IMPORT_ID`) REFERENCES `IMPORT` (`ID`);

ALTER TABLE `EXPORT` ADD CONSTRAINT `CUSTOMER_ID_FK` FOREIGN KEY (`CUSTOMER_ID`) REFERENCES `CUSTOMERS` (`ID`);

ALTER TABLE `EXPORT` ADD CONSTRAINT `EMPLOYEE_ID_FK` FOREIGN KEY (`EMPLOYEE_ID`) REFERENCES `EMPLOYEES` (`ID`);

ALTER TABLE `EXPORT` ADD CONSTRAINT `DISCOUNT_ID_FK` FOREIGN KEY (`DISCOUNT_ID`) REFERENCES `DISCOUNTS` (`ID`);

ALTER TABLE `EXPORT_DEATAIL` ADD CONSTRAINT `PRODUCT_ID_FK` FOREIGN KEY (`PRODUCT_ID`) REFERENCES `PRODUCTS` (`ID`);

ALTER TABLE `EXPORT_DEATAIL` ADD CONSTRAINT `EXPORT_ID_FK` FOREIGN KEY (`EXPORT_ID`) REFERENCES `EXPORT` (`ID`);

ALTER TABLE `ACCOUNTS` ADD CONSTRAINT `USERNAME_FK` FOREIGN KEY (`USERNAME`) REFERENCES `EMPLOYEES` (`ID`);

ALTER TABLE `ACCOUNTS` ADD CONSTRAINT `ROLE_ID_FK` FOREIGN KEY (`ROLE_ID`) REFERENCES `ROLES` (`ID`);
